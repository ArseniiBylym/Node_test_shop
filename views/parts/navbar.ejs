<nav class="navbar navbar-expand-lg navbar-dark bg-dark py-0">
    <a class="navbar-brand" href="/">
        <img src="/images/logo.png" width="50" height="50" class="d-inline-block align-top py-0" alt="">
    </a>

    <div class="collapse navbar-collapse" id="navbarContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="<%= path === '/products' ? 'nav-link active' : 'nav-link' %>" href="/products">Products</a>
            </li>
            <% if (isAdmin) { %>
                <li class="nav-item">
                    <a class="<%= path === '/admin/add-products' ? 'nav-link active' : 'nav-link' %>" href="/admin/add-products">Add product</a>
                </li>
            <% } %>
        </ul>
        <% if (!isAuth) { %>
            <a href="/login">
                <button class="<%= path === '/login' ? 'btn btn-outline-secondary active' : 'btn btn-outline-secondary' %>">Log in</button>
            </a>
        <% } else { %>
            <img src="/images/user.svg" style="width: 20px; height: 20px" class="text-primary w-40 h-40">
                <p class="text-white my-0 ml-1 mr-4">
                    <% if(user) { %>
                    <%= user.email %>
                    <% } %>
                </p>
            <a href="/cart"><img src="/images/shopping-cart.svg" style="width: 20px; height: 20px" class="text-primary w-40 h-40">
                <span class=" mr-4 badge badge-pill badge-light">
                    <% if (user) { %>
                    <%= user.cart.length %>
                    <% } else { %>
                        0
                    <% } %>
                </span>
            </a>
            <a href="/logout">
                <button class="btn btn-outline-secondary">Log out</button>
            </a>
        <% } %>
    </div>
</nav>